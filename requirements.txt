# --- core (always needed) ---
redis==5.0.4
fakeredis==2.23.5             # in-process event bus for run_local.py (no Redis daemon required)
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
pydantic==2.8.2
python-dateutil==2.9.0
requests==2.32.3
websockets==12.0

# --- pipeline A (logs) ---
watchdog==4.0.2

# --- pipeline B (video — motion-detection engine, no model needed) ---
# Pins below have native arm64 wheels for Apple Silicon (M1/M2/M3).
opencv-python==4.10.0.84
numpy==1.26.4

# --- optional: YOLO video engine (uncomment to enable) ---
# Adds ~500 MB. Only needed if you set VIDEO_ENGINE=yolo. Default is mock (motion).
# ultralytics==8.2.103
# torch==2.2.2
